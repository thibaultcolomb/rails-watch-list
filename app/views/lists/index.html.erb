  <div class="banner d-flex justify-content-center align-items-center">
    <h2 class="text-white">Save any kind of movie</h2>
  </div>
<div class="container">

  <div class="row ">
    <div class= "d-flex justify-content-between align-items-center">
      <h3>My lists :</h3>
      <%= link_to "Create a movie list", new_list_path, class: "btn btn-success text-white"%>
    </div>
    <ul class="list-unstyled d-flex justify-content-between mt-3">
     <% @lists.each do |list| %>
       <li class="col-3  card mb-3">
        <%= link_to list.name, list_path(list), class: "text-decoration-none"%>
       </li>
     <% end %>
    </ul>
  </div>

</div>
